#!/bin/bash

# Скрипт для автоматического деплоя на виртуальную машину
echo "🚀 Начинаем деплой на виртуальную машину..."

# Проверяем, есть ли изменения для коммита
if git diff-index --quiet HEAD --; then
    echo "📝 Нет изменений для коммита"
else
    echo "📝 Добавляем все изменения..."
    git add .
    
    echo "💾 Создаем коммит..."
    git commit -m "🔄 Автоматическое обновление: $(date '+%Y-%m-%d %H:%M:%S')"
    
    echo "📤 Отправляем изменения на сервер..."
    git push origin main
    
    if [ $? -eq 0 ]; then
        echo "✅ Деплой успешно завершен!"
        echo "🌐 Приложение будет доступно после обновления на сервере"
    else
        echo "❌ Ошибка при отправке изменений"
        exit 1
    fi
fi

echo "🎉 Готово!" 